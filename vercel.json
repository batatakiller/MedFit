{
  "buildCommand": "npm run build",
  "devCommand": "npm run dev",
  "installCommand": "npm install",
  "framework": "nextjs",
  "regions": ["sfo1"],
  "env": {
    "NEXT_PUBLIC_SUPABASE_URL": {
      "description": "Supabase project URL"
    },
    "NEXT_PUBLIC_SUPABASE_ANON_KEY": {
      "description": "Supabase anonymous public key"
    },
    "SUPABASE_SERVICE_ROLE_KEY": {
      "description": "Supabase service role key (for server-side operations)"
    },
    "ANTHROPIC_API_KEY": {
      "description": "Anthropic API key for Claude models"
    },
    "MEDFIT_AI_MODEL": {
      "description": "Claude model ID (e.g., claude-haiku-4-5)",
      "value": "claude-haiku-4-5"
    },
    "MEDFIT_EMBED_FUNCTION_URL": {
      "description": "Optional: URL to embedding function (leave empty for now)"
    },
    "MEDFIT_SMPL_SERVICE_URL": {
      "description": "Optional: URL to SMPL 3D reconstruction service (future)"
    },
    "NEXT_PUBLIC_APP_URL": {
      "description": "Production app URL"
    }
  },
  "git": {
    "deploymentEnabled": true
  },
  "serverlessFunctionMaxDuration": 60
}
